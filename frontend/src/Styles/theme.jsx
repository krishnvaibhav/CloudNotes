import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    primary: {
      main: "#6B3FA0",
    },
    secondary: {
      main: "#D4AF37",
    },
    otherColor: {
      main: "#fff",
    },
    black: {
      main: "#000",
    },
  },
});
